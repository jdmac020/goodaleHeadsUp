import { expect } from 'chai';

describe('Example tests', () => {

    it('Is True', () => {
      expect(true).to.eql(true);
    });

    it('Is False', () => {
      expect(false).to.eql(false);
    });
  });